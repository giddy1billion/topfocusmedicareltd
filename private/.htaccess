# Deny all direct web access to the enquiry data store.
# The enquiries.jsonl file holds patient-submitted PII and must never be served.
Require all denied
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
